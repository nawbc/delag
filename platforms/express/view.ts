export interface ViewOptions {}

export class View {
  constructor(name: string, options?: ViewOptions) {}

  private lookup() {}

  /**
   * Render with the given options.
   *
   * @param options
   * @param callback
   */
  private render(options: any, callback: any) {}
}
